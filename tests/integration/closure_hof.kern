fn make_adder(n: i64) -> fn(i64) -> i64 {
    { x: i64 => x + n }
}

fn main() -> i64 {
    val add10: fn(i64) -> i64 = make_adder(10)
    val add20: fn(i64) -> i64 = make_adder(20)
    add10(12) + add20(0)
}
