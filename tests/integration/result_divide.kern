union Result { Ok(i64), Err(i64) }

fn divide(a: i64, b: i64) -> Result {
    if b == 0 {
        Result::Err(0)
    } else {
        Result::Ok(a / b)
    }
}

fn main() -> i64 {
    val r: Result = divide(84, 2)
    match r {
        Ok(v) => v
        Err(e) => e
    }
}
