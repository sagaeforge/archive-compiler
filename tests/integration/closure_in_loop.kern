fn apply(f: fn(i64) -> i64, x: i64) -> i64 { f(x) }

fn main() -> i64 {
    val inc: fn(i64) -> i64 = { x: i64 => x + 1 }
    loop(n = 0) {
        if n >= 42 {
            break n
        }
        continue(apply(inc, n))
    }
}
