enum Color { Red, Green, Blue }

fn color_to_int(c: Color) -> i64 {
    match c {
        Red => 10,
        Green => 20,
        Blue => 12
    }
}

fn main() -> i64 {
    color_to_int(Color.Blue) + color_to_int(Color.Green) + color_to_int(Color.Red)
}
