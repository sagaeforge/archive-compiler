// Tail call in both if/else branches
fn double_it(x: i64) -> i64 { x + x }
fn triple_it(x: i64) -> i64 { x + x + x }

fn choose(x: i64) -> i64 {
    if x > 10 { double_it(x) }
    else { triple_it(x) }
}

fn main() -> i64 {
    choose(21)
}
