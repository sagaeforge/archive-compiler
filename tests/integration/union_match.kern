union Shape { Circle(i64), Square(i64) }

fn area(s: Shape) -> i64 {
    match s {
        Circle(r) => r * r * 3,
        Square(side) => side * side
    }
}

fn main() -> i64 {
    area(Shape::Square(5)) + area(Shape::Circle(3)) - 10
}
