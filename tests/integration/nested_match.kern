union Option { Some(i64), None(i64) }

fn main() -> i64 {
    val a: Option = Option::Some(2)
    val b: Option = Option::Some(21)
    match a {
        Some(x) => match b {
            Some(y) => x * y
            None(_) => 0
        }
        None(_) => 0
    }
}
