// Test: nested closures — inner captures outer's captured var
fn main() -> i64 {
    val a: i64 = 10
    val outer: fn(i64) -> i64 = { x: i64 =>
        val b: i64 = x + a
        val inner: fn(i64) -> i64 = { y: i64 => y + b }
        inner(2)
    }
    outer(30)
}
