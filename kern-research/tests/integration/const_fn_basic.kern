@const
fn add(a: i64, b: i64) -> i64 {
    a + b
}

@const
fn double(x: i64) -> i64 {
    x * 2
}

static_assert(add(10, 11) == 21, "const fn add should work")
static_assert(double(21) == 42, "const fn double should work")

fn main() -> i64 {
    add(20, double(11))
}
