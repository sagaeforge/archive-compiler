fn accumulate(n: i64) -> i64 {
    loop(sum = 0, count = 0) {
        if count >= n {
            break sum + count
        }
        continue(sum + count, count + 1)
    }
}

fn main() -> i64 {
    accumulate(7)
}
