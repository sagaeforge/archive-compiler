@repr(u8)
enum Opcode {
    Nop = 0,
    Load = 1,
    Store = 2,
    Halt = 255
}

@repr(u32)
enum Error {
    Ok = 0,
    NotFound = 2,
    Denied = 13
}

fn main() -> i64 {
    val s1: i64 = sizeof(Opcode) as i64
    val s2: i64 = sizeof(Error) as i64
    s1 + s2 * 10
}
