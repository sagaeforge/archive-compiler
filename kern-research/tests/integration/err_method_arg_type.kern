struct Counter { var value: i64 }

impl Counter for Counter {
    fn add(self: Counter, amount: i64) -> i64 { self.value + amount }
}

fn main() -> i64 {
    var c: Counter = Counter { value: 10 }
    c.add(true)
}
