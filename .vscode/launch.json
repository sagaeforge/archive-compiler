{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug with LLDB",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "terminal": "integrated",
      "console": "integratedTerminal",
      "initCommands": [
        "command script import ${workspaceFolder}/external/lldb/icu_unicode_string_formatter.py"
      ]
    }
  ]
}