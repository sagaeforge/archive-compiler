struct complex(
  let real: Float32,
  let imaginary: Float32
);

fn power(complex: Complex, power: Float32): Complex {
  return {
    real: complex.real * power,
    imaginary: complex.imaginary * power
  }
}

let num = Complex { real: 5.1, imaginary: 2.1 }
power(num, 2) 