모든 값은 모나드로 표현된다.

함수는 정적 시간을 가진 함수와, 모나드를 가진 비동기형 코루틴을 지원한다.


var A = 10; // Variable<Int, NumericException, ImmutablePolicy>(10)
var B = "abc"; // Variable<String, StringException, ImmutablePolicy>("abc")

ptr fn = (a: Int) { // fn: Pointer<Function, FunctionException, ImmutablePolicy> a: Variable<Int, NumericException, ImmutablePolicy>
  if (a.hasException())
    return NumericOverflowException();

  return a + 20;
}

Object, Struct, Union, Enum

Object는 Object/Struct 소유 가능, 메소드 함수 제공
Struct는 정보만 소유 가능, 프로퍼티 문법만 제공, Union과 Enum을 소유할 수 있음.

책임을 나눠 실수를 방지하는 목적임.

그리고 전위 / 후위 연산자 오버라이딩

일단, 지금 구조도 좋은데, 지금 구조에서 가장 큰 문제는 더럽게 복잡하다는 거지.

테스트 코드를 넣어서 한번에 처리하는 게 이상적인 구조가 될 것 같음.

그런 의미로 오늘은 치트키 한번 쓰겠습니다.