# Kern Language — Requirements Specification

> 생성일: 2026-03-01
> 최종 수정: 2026-03-01
> 상태: Draft v2 (SYNTAX.md와 동기화됨)

---

## 1. 비전 & 목표

### 비전
하드웨어를 조작하는 복잡한 프로그램을 **읽기 쉬운 코드**로 만든다.

### 핵심 목표
- 수학적으로 순수한, 기초적인 언어 시스템 구현
- 순수 함수의 집합으로 **OS 커널**을 개발할 수 있는 언어
- **컴퓨팅 체인**: 프로그래머가 병렬을 지시하지 않아도, 기계어 수준에서 함수를 자동으로 노드에 분배·조합·전달하는 실행 모델

### 컴퓨팅 체인 개념
- 프로그래머는 순수 함수를 작성
- 런타임/컴파일러가 함수의 순수성을 보장하고, 자동으로 실행 노드를 결정
- **노드**: 추상화된 개념 — CPU 코어일 수도, 네트워크 상의 다른 머신일 수도 있음
- 함수 실행 → 결과 조합 → 다음 함수 전달의 체인 구조

### 핵심 원칙: "모호하면 에러"
- 컴파일러/파서는 **절대로 추측하지 않는다**
- 모호한 코드는 즉시 컴파일 에러 발생
- 모든 에러 메시지에 **상세한 설명 + 해결 방법(help)**을 함께 제시
- 프로그래머가 의도를 명확히 표현하도록 유도

---

## 2. 기능 요구사항 (Functional Requirements)

### FR-1: 순수 함수 체계 — 컴파일러 자동 추론
- 순수성은 **컴파일러가 함수 본문을 분석하여 자동 추론** — 프로그래머가 선언하지 않음
- 어노테이션 없음: `@` 자체가 언어에 존재하지 않음
- 순수성 판단 기준:
  - `var` 미사용 + 불순 함수 미호출 + 외부 상태 미접근 → `pure`
  - `var` 사용 → `impure(mut)` (해당 함수에만 적용, 호출자에게 전파되지 않음)
  - intrinsic/IO 함수 호출 → `impure(io)` (호출자에게 전파됨)
  - 메모리 할당/해제 함수 호출 → `impure(mem)` (호출자에게 전파됨)
- **전파 규칙**: `impure(io)`와 `impure(mem)`는 호출 체인을 따라 전파됨. `impure(mut)`는 전파되지 않음 (로컬 변이는 외부에서 관찰 불가)
- LSP가 추론된 속성을 인레이 힌트, 색상 구분으로 시각화

### FR-2: 타입 시스템
- **강타입(Strong typing) + 명시적 타입 선언**
- `val` / `var` 바인딩 (Kotlin 스타일):
  - `val` — 불변. 한 번 바인딩되면 변경 불가
  - `var` — 가변. 재할당 가능. 함수가 `impure(mut)`가 됨
- 타입은 항상 명시적: `이름: 타입 = 값`
- 커널 개발을 위한 예측 가능한 타입 동작
- 기본 타입: 정수(i8/i16/i32/i64, u8/u16/u32/u64), 부동소수점(f32/f64), 불리언(bool), 유닛(Unit), 포인터(Ptr<T>), 참조(Ref<T>)

### FR-3: 메모리 관리
- **수동 관리 기반 + 소유권 개념 혼합**
- 기본적으로 수동 할당/해제 (malloc/free 수준의 제어)
- 소유권 시스템으로 안전성 보강 (Rust 영향)
- 커널 개발에 적합한 저수준 메모리 접근 가능

### FR-4: 모나드 — 독립적 값 타입
- `Result<T, E>` — 성공 또는 실패를 담는 컨테이너
- `Maybe<T>` — 값이 있거나 없음
- 모나드는 **이펙트 시스템과 무관한 독립적 값 타입** — 순수 함수에서도 자유롭게 사용 가능
- 명시적 try-catch 구조 없음 — 모나드 + `|> bind` 체이닝으로 오류 처리
- `@Effect` 등 어노테이션 없음 — 순수성과 이펙트는 전부 컴파일러 자동 추론
- 런타임 구현: Tagged Union (태그 1바이트 + union 크기, 힙 할당 없음)

### FR-5: 문법 체계 — 설계 완료
- **Kotlin 베이스** 문법 (전체적 느낌과 인체공학)
- 함수 선언: `fn name(args) -> Type { body }`
- 바인딩: `val` (불변) / `var` (가변, 경고 발생)
- 파이프 연산자: `|>` — `a |> f(b)` = `f(a, b)` (데이터 흐름을 왼쪽→오른쪽으로)
- 패턴 매칭: `match` 표현식 + 함수 정의 수준 패턴 매칭
- 논리 연산: `and` / `or` / `not` 키워드 (`&&` / `||` / `!` 없음)
- 블록: `{ }` 중괄호 (마지막 표현식이 반환값)
- 세미콜론: 선택적 (한 줄에 복수 문장일 때만 사용)
- 포인터: `*ptr` 역참조, `&x` 참조 획득 (자동 역참조 없음, `(*ptr).field` 필수)
- 제네릭: `<>` 타입 위치 전용 (터보피시 `::<>` 없음, 표현식 위치는 추론 전용)
- 어노테이션: **없음** (`@` 자체가 언어에 존재하지 않음)
- 주석: `//` 한 줄 + `/* */` 블록
- `return`: 선택적 (마지막 표현식이 반환값으로 유효)
- 상세 문법 명세: `docs/SYNTAX.md` 참조

### FR-6: 컴파일 파이프라인
- **소스코드 → 자체 IR → 어셈블리(NASM) → 네이티브 바이너리**
- 자체 IR에 순수성 정보 및 분배 가능성 메타데이터 포함 (컴퓨팅 체인 대비)
- 첫 타겟: **macOS native** (`nasm -f macho64` + `ld`)

---

## 3. 비기능 요구사항 (Non-Functional Requirements)

### NFR-1: 성능
- 커널 개발이 가능한 수준의 성능 (C/Rust 급)
- 런타임 오버헤드 최소화
- GC 없음 (커널 환경에서 GC 불가)
- 제로 코스트 추상화: 파이프, 패턴 매칭, 제네릭 등 모든 추상화가 컴파일 타임에 해소

### NFR-2: 안전성
- 컴파일 타임 순수성 자동 추론 및 검증
- 타입 안전성 보장
- 메모리 안전성은 소유권 시스템으로 보강

### NFR-3: 구현 언어 & 빌드
- 컴파일러 구현 언어: **C++20**
- 빌드 시스템: **CMake**
- 개발자 경험 수준: 중급 이상 (컴파일러 개발 경험 있음)

---

## 4. 마일스톤

### M1: 피보나치 재귀
- 단일 노드에서 동작
- 컴퓨팅 체인 미포함
- 기본 컴파일 파이프라인 완성 (렉서 → 파서 → IR → 코드젠)
- 재귀 함수 호출 동작
- macOS native 바이너리 생성 (`nasm -f macho64` + `ld`)

### M2 (예상): 기본 타입 시스템 + 순수성 자동 추론
- 강타입 시스템 구현
- 컴파일러 순수성 자동 추론 엔진 (함수 본문 분석 → pure / impure(mut/io/mem) 판정)
- `var` 사용 시 컴파일 경고 시스템 (`val` + 재귀로 리팩터링 제안)
- IR에 순수성 메타데이터 추가

### M3 (예상): 메모리 관리 + 사이드 이펙트
- 수동 메모리 관리 구현
- 소유권 시스템 기초
- 사이드 이펙트 처리 방식 결정 및 구현

### M4 (예상): 컴퓨팅 체인 기초
- IR의 분배 가능성 메타데이터 활용
- 단일 머신 내 멀티코어 분배
- 이후 네트워크 노드로 확장

---

## 5. 사용자 스토리 / 수용 기준

### US-1: 기본 프로그램 컴파일
> Kern 개발자로서, 피보나치 재귀 함수를 작성하고 컴파일하여 macOS에서 실행할 수 있다.
- [ ] 소스 파일을 입력으로 받음
- [ ] 렉싱 → 파싱 → IR 생성 → 코드 생성 파이프라인 동작
- [ ] 생성된 바이너리가 macOS (amd64)에서 실행됨
- [ ] 재귀 함수 호출이 정상 동작

### US-2: 순수성 자동 추론
> Kern 개발자로서, 컴파일러가 자동으로 함수의 순수성을 추론하고, `var` 사용 시 경고를 표시한다.
- [ ] 컴파일러가 함수 본문을 분석하여 순수성을 자동 추론
- [ ] `var` 사용 함수에 `impure(mut)` 경고 표시 + `val` + 재귀로의 리팩터링 제안
- [ ] `impure(io)` / `impure(mem)`는 호출 체인을 따라 전파
- [ ] LSP에서 추론된 속성 인레이 힌트 표시

### US-3: 커널 코드 작성
> Kern 개발자로서, 하드웨어 레지스터에 접근하는 커널 코드를 안전하게 작성할 수 있다.
- [ ] 저수준 메모리 접근 가능
- [ ] intrinsic 함수를 통한 하드웨어 접근 (불순의 원천으로 컴파일러가 인식)
- [ ] 사이드 이펙트가 컴파일러 자동 추론에 반영됨

---

## 6. 미결 사항 (Open Questions)

| # | 질문 | 상태 |
|---|------|------|
| ~~OQ-1~~ | ~~사이드 이펙트 처리 방식: IO 모나드 vs Effect System vs Unsafe 블록?~~ | **결정됨**: 컴파일러가 모든 이펙트를 자동 추론. 명시적 이펙트 시스템 없음 |
| ~~OQ-2~~ | ~~독자적 문법의 구체적 형태는?~~ | **결정됨**: Kotlin 베이스 문법 설계 완료. `docs/SYNTAX.md` 참조 |
| OQ-3 | 소유권 시스템의 구체적 범위와 규칙은? (Rust만큼 엄격? 더 유연?) | 미정 |
| ~~OQ-4~~ | ~~M1 마일스톤 날짜 재조정 필요~~ | **결정됨**: 날짜 제거. 기능 완성 기준으로 관리 |
| ~~OQ-5~~ | ~~모나드의 구체적 타입 설계~~ | **결정됨**: `Result<T, E>`와 `Maybe<T>`를 독립적 값 타입으로 설계. 이펙트와 무관 |
| OQ-6 | 컴퓨팅 체인의 런타임 vs 컴파일타임 분배 결정 비중은? | 미정 |
| OQ-7 | 부트스트래핑 계획: Kern으로 Kern 컴파일러를 재작성할 계획이 있는가? | 미정 |

---

## 7. 다음 단계

1. `ARCHITECTURE.md` 토큰/AST 업데이트 (`val`/`var`/`|>`/`and`/`or`/`not`/`&`/`*` 추가)
2. M1 구현 워크플로우 작성
3. 미결 사항(OQ-3, OQ-6, OQ-7) 순차 결정
